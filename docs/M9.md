# M9 — Parity Audit (final migration gate)

**Status:** complete — audit found 7 issues (3 cutover-blocking), all fixed and committed. 236/236 tests, fmt/clippy/check clean.

Method: full-surface inventory of the TS oracle (@explorer) — commands, callbacks, startup sequence, env vars, timers, DB schema, settings fields, help text — diffed against the Rust implementation, then gap-by-gap verification and fix.

## Verified-matching surface

| Area | Result |
|---|---|
| Commands (22 registrations, incl. all aliases) | ✅ 1:1 — alac/rip/batch/dl/download/rerip/cache/dump, cancel, help, ping, health, queue, stats, status, info, search, random, spec/spectogram/spectrogram/spek, report/issue, export, import, dumpnew/autodump, settings, auth, revoke/unauth, authlist, clean, delete, index, start |
| Callback prefixes | ✅ dl:/rip:/search_close, random:, report:, cancel:, dashboard: (Rust name for TS `status:` — recorded M5 deviation), settings:, authpage:/authclose/noop |
| `/start` = `Hello, world!` | ✅ verbatim (not a placeholder) |
| Help text | ✅ byte-for-byte (programmatically diffed: general + admin blocks) |
| Settings fields (9) + defaults | ✅ exact |
| DB schema (drizzle 0000+0002) | ✅ exact — tables, columns, constraints, indexes |
| Startup sequence | ✅ tracing → migrations → settings init → client → dispatcher → handlers → scheduler; SIGINT/SIGTERM shutdown |
| Background jobs | ✅ only the 24h autodump scheduler (M8) — no others in oracle |
| No inline queries / webhooks / HTTP servers in oracle | ✅ confirmed; none in Rust |

## Findings & fixes

| # | Severity | Finding | Fix |
|---|---|---|---|
| F1 | 🔴 blocker | Env var name mismatch: TS reads `ALAC_API_KEY`, Rust read `ALAC_MIRROR_API_KEY` — on cutover the mirror would silently have no auth | Read `ALAC_API_KEY` (`rip_deps.rs`) |
| F2 | 🟠 | TS defaults `ALAC_WRAPPER_URL=http://127.0.0.1:12340`; Rust treated missing as None → wrapper fallback disabled entirely | Default applied (`rip_deps.rs`) |
| F3 | 🟠 | `ALAC_MAX_RETRIES` ignored (hardcoded 3); `ALAC_RETRY_BASE_MS` default 3000 vs TS 2000 | `upload_max_retries()` env override + base 2000 (`rip_deps.rs`) |
| F4 | 🔴 blocker | Drizzle 0001 (pg_trgm extension + trgm index) missing from Rust migrations | New `trgm_extension` welds step, idempotent (`db/migrations.rs`) |
| F5 | 🟠 | `search_cached_tracks` lacked TS's pg_trgm `word_similarity(...) >= 0.35` fuzzy clause + `similarity DESC` secondary sort — search recall/quality gap | Full clause + sort added (`db/tracks.rs`), verified against the live extension |
| F6 | 🟡 | `RUST_LOG` fallback missing from log-level resolution (TS: `LOG_LEVEL || RUST_LOG || 'info'`) | Fallback added (`main.rs`) |
| F7 | 🔴 blocker | Cutover: production DB is drizzle-migrated; welds uses its own `_welds_migrations` bookkeeping → first Rust start would try `CREATE TABLE` on existing tables and abort | All migration statements idempotent (`IF NOT EXISTS`); **verified**: dropped bookkeeping table, single-process migrate succeeds, restarts no-op |

## Residual deviations (accepted, previously recorded)

- `dashboard:` vs TS `status:` callback prefix (M5 architecture rename, user-visible behavior identical).
- M5c/M6/M7/M8 deviations as recorded in their docs (collapsed-pipeline rendering, retry-exhaustion record-and-continue, single-message group completion, /spec WAV fallback, /info full-row lookup, detached /index progress edits, autodump/random/search orchestrator mappings).

## Cutover readiness

- Command surface, schema, settings, env contract now match the oracle.
- Verified migration paths: fresh DB (full schema) ✅; drizzle-migrated DB (no-op start) ✅; restart idempotency ✅.
- Remaining for cutover (M10): Dockerfile for the Rust workspace (oracle ships ffmpeg/sox system deps), systemd/service unit, README/runbook update, TS sunset.
