# M10 — Cutover Prep (deployment + TS sunset)

**Status:** complete.

## Deliverables

- **justfile** — task runner; stable toolchain for build/check/clippy/test, `+nightly` only for `fmt`/`fmt-check` (import grouping/sorting is a nightly rustfmt option).
- **rust-toolchain.toml removed** — repo no longer pins nightly; stable is the default toolchain.
- **Dockerfile** — rust:1-slim builder (stable, layer-cached manifests/stubs) → debian:bookworm-slim runner with the oracle's system deps (ffmpeg, sox, libsox-fmt-all, ca-certificates), non-root `alac` user, `/app/bot-data` volume, migrations at startup. (Image build validated — runtime smoke test remains.)
- **README.md** — rewritten for the Rust runtime: prerequisites (Rust/just/FFmpeg/SoX/PostgreSQL), quick start (`just run`), Docker usage, full env-var table (now including `ALAC_MAX_RETRIES`/`ALAC_RETRY_BASE_MS`), command reference, development recipes.
- **.env.example** — cleaned: PGlite/`DATABASE_DIR` block removed (PostgreSQL only), TS-era comments updated.
- **.dockerignore** — Rust context (source/crates/lockfile only).
- **.gitignore** — Rust-only tree (`/target`, `/bot-data`) plus the worktree block.
- **`DATABASE_URL` default parity** (`crates/bot/src/main.rs`): TS `env.ts` defaults it to `postgresql://admin:password@localhost:5432/alac_bot`; the Rust loader panicked when unset — now defaults identically.
- **zod.rs evaluated and rejected** — the hand-rolled env loader (~30 lines) already matches the TS contract; a young validation crate adds risk for zero behavioral gain (workspace policy: pin young crates only behind seams).

## Toolchain decisions (user-directed)

- No compose file — Dockerfile only.
- No pinned toolchain file; nightly remains a dev-only concern (formatting).
- No PGlite — PostgreSQL only.

## Deferred

- Container runtime smoke test (image builds; `docker run` against a live bot not yet exercised).

## TS sunset

- `typescript` branch created from `main` at the last TS-era commit (`165bac7`) — the complete, runnable TS implementation preserved as a branch.
- TS surface removed from `rust-migration` (and merged to `main`): `src/`, `tests/`, `drizzle/`, `drizzle.config.ts`, `package.json`, `bun.lock`, `.npmrc`, `biome.json`, `tsconfig.json`. TS files also remain reachable in git history.
- Migration + parity records live in `docs/M1.md`–`docs/M10.md`; the TS oracle they reference is readable on the `typescript` branch or under `docs/oracle-snapshot/`.
